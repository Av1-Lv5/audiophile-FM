import heroProduct from "./hero-product-schema";
import includedItems from "./included-items-schema";
import product from "./product-schema";

const schemas = [product, includedItems, heroProduct];

export default schemas;
